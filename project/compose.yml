version: '3'

services:
  vite_docker:
    image: frontend-vite-server
    ports:
      - "5173:5173"
    volumes:
      - "./frontend/vite-project:/srv/app" 
    container_name: vite-server
    working_dir: /srv/app

  flask_server:
    image: flask-server
    ports:
      - "5000:5000"
    volumes:
      - "./internals/:/srv/app"
    container_name: flask-server
    working_dir: /srv/app

  my_redis:
    image: redis
    ports:
      - "6379:6379"
    container_name: myredis
